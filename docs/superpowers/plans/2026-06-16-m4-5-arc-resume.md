# M4.5 / M4.6 arc — current resume state (2026-06-16, for a FRESH session)

> **Read this first** in the new session. You are **Claude Coordinator** orchestrating the M4.5 (68000 interpreter)
> → M4.6 (JIT) → benchmarking arc. This supersedes `2026-06-16-overnight-progress-resume.md` (a historical snapshot).
> Authored after a long session that hit the empty-spawn glitch repeatedly — the fresh session + the settings change
> below should clear it.

## ✅ Merged to main (each independently verified green before merge)
- **PR #49** — TEST-INFRA: 68000 sweep sampling + the teardown-flake fix. Merged `323db34` (2026-06-17). The 68000
  TomHarte sweep now defaults to **200 cases/file** (`ResolveSampleSize()`, like 6502/Z80; `CPUEMULATOR_TOMHARTE_SAMPLE`
  override; **`CPUEMULATOR_UAT=full` = the full ~8065/file sweep**). Root-caused + fixed the recurring test-host
  teardown abort: `FieldGrammarCliTests` called `Program.Main` in-process without redirecting `Console.Err`, leaking
  `error:` to the host stderr during parallel teardown → vstest "host crashed" → nonzero exit after all-pass (the same
  `--config`/`--field-grammar` abort seen since PR #44). **Effect: routine full gate ~23m → ~12m, clean exit.**
  **VERIFICATION POLICY (Coordinator):** additive / byte-identity-gated PRs (incl. M5 steps — proven by `RegeneratedSpec`
  green that 6502/Z80/68000 emission is unchanged) verify with the **fast sampled** gate + `RegeneratedSpec`; the
  **`UAT=full` full sweep is reserved for PRs that actually CHANGE 68000 behavior** (e.g. the deferred 2b-continuation).
  A further lever (deferred): **C** — cut `M68000TomHarteLoader` per-case allocation (the memory-bandwidth bottleneck).
- **PR #48** — M5.1: the 8086 STATE foundation. Merged `bc2464c` (2026-06-17). New `CpuEmulator.Cpus.M8086` project +
  hand-authored `M8086Spec` (AX/AH/AL pair-views, SP/BP/SI/DI, segment regs, IP, FLAGS), `Flag` enum +T/+Df (AF reuses
  H), `M8086Cpu` skeleton, 20-bit LE bus. `CpuEmitter` UNTOUCHED; `RegeneratedSpec` byte-identity green. Additive.
- **PR #40** — M4.5b (integer ALU). 
- **PR #41** — cross-CPU benchmarking Milestone A (6502+Z80 baseline, both tiers + C#/C/JS Z80 refs; W1/W2 constants frozen as the M6 re-measure contract).
- **PR #42** — M4.5c (shift/rotate/bit/BCD/Scc/CMPM/data-movement+MOVEP).
- **PR #43** — M4.5d-1 (control flow + the exception model). The 68000 is now FUNCTIONALLY complete.
- **PR #47** — M4.5d-2b FOUNDATION (the deferred-refill prefetch seam + cycle-exact timing for 13 families).
  Merged `637f13a` (2026-06-17). Coordinator-verified clean: full Release **5761/0/0** (22m54s, no hang). Shipped:
  `NextUnit` untraced peek + backlogged refills so refill reads interleave between operand accesses (the
  architecturally-novel core); the `SeedPeek` 2a-trace-pollution fix; 13 families cycle-exact (NOP, SWAP, MOVE.q,
  LEA, TST.w, CLR.b, NEG.w, CMP.w/.b, AND.b, OR.w, ADD.b, SUB.b). Built in PARALLEL with M4.6 (worktree-isolated),
  merged after a fixer pass (generator self-containment — emit `_pendingIdle`/`_reseededInBody`/`IdleCycles`/
  `FlushRefills` in the FieldGrammar block; + the 0-cycle taken-transfer hazard that broke M4.6's budget=1 JIT
  invariant and caused a 10h hung gate — fixed by charging `PendingRefills*4` before `DiscardRefills`).
  **DEFERRED to a 2b-continuation/2c (honest per DD6):** T7 full IPL, T8 address-error group-0 frame, T9 full
  timing-sweep flip, and the resistant families (.l-register ALU idle, ADDQ/SUBQ-to-An, two-EA MOVE, MOVEM, MUL/DIV,
  control-transfer reseed). **CpuEmitter is now FREE → M5 (8086) is unblocked to implement.**
- **PR #46** — M4.6 (the 68000 through the JIT — all-fallback tier parity, data axis). Merged `3f7ba7f`.
  Coordinator-verified clean: full Release **5747/0/0** (+128 = the new JIT-parity tests). PURE PLUMBING (the
  tripwire stayed minimal — no dispatcher widen; deferred to M6): GAP1 `M68000Cpu.Jit.cs` (`AdvanceCycles`), GAP2
  `[assembly: InternalsVisibleTo]`, GAP3 documented `ushort`-key limitation + guard. The implementer caught + fixed
  a real budget-loop bug (a block now runs exactly ONE instruction via a 1-cycle budget). `RunCaseThroughJit` added
  additively to `M68000TomHarteRunner`. Built in PARALLEL with M4.5d-2b (worktree-isolated).
- **PR #45** — parallelize the TomHarte sweep (per-file/per-plane collections + a `[ThreadStatic]` per-thread
  arena in `M68000TomHarteRunner`). Merged `84abb5e`. The full Release gate is now **~22.5 min** (was ~33), cap=16,
  byte-identical (5619/0/0). Test-infra only; `src/` untouched. See "Next steps" §2 for the GC-bound finding.
- **PR #44** — M4.5d-2a (the prefetch-queue SEAM-BREAK + the PC/prefetch axis). Merged `e7ef251` (2026-06-16).
  Coordinator-verified clean: full Release suite **5613 passed / 0 failed / 0 skipped**, exit 0; the 2a PC/prefetch
  sweep green; 6502/Z80 byte-identical; M4.5d-1 data-axis gate byte-identical. Pre-merge review found no critical
  issues; two findings folded in before merge: **MOVEQ** classified EA-less (same over-read class as MOVE_USP +
  the register shifts — `MOVE.q.json.gz` added to the 2a sweep to gate it) and the stale `timingAxis:true` USP
  bonus-gate comment corrected. NOTE: the earlier-session "2216/2216" figure was the pre-2a count; 2a + the MOVEQ
  gate raised it to 5613. The address-error frame PC/SR/SSW/access-address stay **trace-coupled + deferred to 2b**.
- main HEAD = `e7ef251`. 6502/Z80 byte-identity intact throughout.

## ⛔ Open + HELD for owner review (NOT merged)
- None. (PR #44 merged — see above.)

## 📄 On main (docs / decided)
- `docs/architecture/0008-…-timing-axis.md` — **ACCEPTED**. Forks resolved: **C** = Tier (i) cycle-accuracy, staged;
  **E** = full IPL in M4.5d-2; **F** = precise address-error frame in M4.5d-2.
- `docs/superpowers/plans/2026-06-16-m4-5d-2-timing-prefetch.md` — the M4.5d-2 plan (2a DONE in PR #44; **2b remains**).
- `docs/superpowers/plans/2026-06-16-m4-6-68000-through-jit.md` — M4.6 plan (**pure plumbing**: 2 ~3-line edits + a
  data-axis JIT parity sweep; depends only on M4.5d-1, so it can run in PARALLEL with M4.5d-2).

## ▶️ Next steps (in order — owner chose the infra PR BEFORE 2b)
1. ✅ **Review + merge PR #44** — DONE (merged `e7ef251`, see above).
2. ✅ **Parallelize the TomHarte sweep** — DONE (PR #45 merged `84abb5e`). Per-file (68000) / per-plane (Z80) /
   4-way (6502) split into separate xUnit collections + `xunit.runner.json` (cap=16). **KEY FINDING:** the split
   ALONE regressed (45m vs 33m) — the 68000 runner allocated a fresh **16 MiB arena per case** → GC thrash; the
   sweep is memory-bandwidth-bound, NOT CPU-bound. Fix: a `[ThreadStatic]` reused arena (`M68000TomHarteRunner`).
   Net **~1.46×** (33m → ~22.5m), byte-identical (5619/0/0; +6 = new coverage guards). 3-5× is NOT reachable here;
   the next lever (if ever wanted) is cutting the per-file JSON-parse allocation in `M68000TomHarteLoader`. The full
   Release gate is now ~22.5 min at cap=16.
3. ✅ **M4.6** (68000 through the JIT) — DONE (PR #46 merged `3f7ba7f`). Ran in PARALLEL with 2b (worktree-isolated).
4. ✅ **M4.5d-2b FOUNDATION** — DONE (PR #47 merged `637f13a`, see above). The deferred-refill seam + 13 cycle-exact
   families landed; the interleaved-refill seam works. (Note: "full 2b in one PR" was the owner's call, but the
   Builder responsibly checkpointed at the foundation — the rest is genuinely multi-session.)
5. **M4.5d-2b CONTINUATION (a.k.a. 2c)** — the deferred work: **T7 full IPL** (synthetic — no corpus vector),
   **T8 address-error group-0 frame** (the interleaved set), **T9 full timing-sweep flip**, and the resistant
   families (.l-register ALU idle, ADDQ/SUBQ-to-An, two-EA MOVE, MOVEM, MUL/DIV, control-transfer reseed). Iterative
   (DD6 — honest per-class deferral). Reconcile PER-FILE (`--filter` = seconds); full timing sweep only at the gate.
   A scratch harness `M68000TimingReconTests.cs` is in-tree as the per-class loop infra. NOT yet scheduled.

## ▶️ The emulated-computer arc (NEW — owner-initiated 2026-06-17)
**Intent:** a composable, CPU-agnostic device/machine TOOLKIT, proven by a PORTFOLIO of real legacy machines.
**Target direction (owner, 2026-06-17) = "reuse + gentle-first":** pick targets for a gentle video-complexity start,
maximal SHARED-CHIP reuse, and a multi-CPU showcase. Working candidates (exact list TBD): a SIMPLE first machine
(e.g. **ZX Spectrum**, Z80); the **MS-DOS PC clone** (8086 — canonical standard chips, the library seed); **C64**
(6502 — VIC-II/SID/dual-6526; C64+1541 = a dual-6502 multi-CPU showcase); a 68000 showcase (e.g. **Sega Genesis** —
68000+Z80 dual-CPU, VDP tile/sprite, FM). **Atari 800** demoted to a LATER advanced-6502 target (ANTIC/GTIA = the deep
end). Reusable chips to factor: MC6845 CRTC, AY-3-8910/YM2149, TMS9918 VDP, 6522 VIA / 6526 CIA. **Surface = web**
(local HTTP+WebSocket → browser canvas + input). Base to extend: `IPeripheral`/`IScheduler`/`IInterruptLine`/
`IMachineContext` + `Machine` + `Breadboard6502`.
**Sequencing (owner chose "foundation + M5 in parallel"):**
- **SP0 — foundation:** web surface + 3 generic contracts (`IDisplayDevice` RGBA framebuffer, `IKeyboardSink`+`KeyEvent`,
  `IBlockDevice` sectors) + a `MachineHost` wall-clock/frame pump + a trivial demo machine. **SPEC WRITTEN + APPROVED:**
  `docs/superpowers/specs/2026-06-17-emulated-computer-sp0-foundation-design.md`. **Implementation DEFERRED until after
  M5 + M6 ship** (owner's call) — no plan yet; `writing-plans` when scheduled. New projects: `CpuEmulator.Surface.Web`,
  `CpuEmulator.Machines`.
- **M5 (8086)** — IN PROGRESS. Plan `2026-06-17-m5-8086-execution.md`. ✅ M5.1 state (#48), ✅ M5.2 x86 decode
  front-end (#50 merged `3560c61`). 🔄 M5.3 EA/segmentation IN PROGRESS (`feat/m5-3-ea-segmentation`; also wires the
  real 20-bit `(CS<<4)+IP` fetch, resolving M5.2's deferred MEDIUM). Then M5.4 importer/dataset/8088 loader → M5.5
  interpreter + the 8088 gate → M5.6 JIT parity → (M5.5e timing, late). **8088 vectors fetched**
  (`~/.cache/cpuemulator/vectors/8088/v2`, 324 files, 727M — via git, not PowerShell). **Sequencing rule: M5
  (CpuEmitter) and the deferred 2b-continuation must NEVER run concurrently.** Per-PR verify uses the FAST sampled gate
  + `RegeneratedSpec` (M5 is byte-identity-gated — it never changes 6502/Z80/68000).
- **M6 (JIT optimizations)** — after M5. Ships before SP0 implementation.
- **SP1+ — the machine portfolio** (after SP0; exact first machine TBD per the reuse+gentle-first direction above —
  likely the simplest, e.g. ZX Spectrum). The PC clone needs M5.
- **Device↔JIT ADR 0009 — ✅ ACCEPTED** (owner-approved 2026-06-17): fastmem device memory (framebuffer = fast
  RAM + async snapshot at FrameReady, NOT per-write traps), a mapping-change → page-level block-invalidation hook for
  bank-switch/mapper devices, and tiered (coarse block-boundary vs fine cycle-exact) timing accuracy. All ADDITIVE
  opt-in `Core` capability interfaces (`IFastMemoryProvider`/`IMapInvalidationListener`/`TimingTier`+`ITimingSensitive`)
  — no change to `IPeripheral`/`IScheduler`, no JIT emit change. Informs SP0's contracts + SP1's peripherals + M6.
- **Machine-definition-format ADR 0010 — ✅ ACCEPTED** (owner-approved 2026-06-17; config-over-code = a HYBRID): a
  declarative JSON manifest (`cpuemu.machine/1`) defines the machine WIRING (CPU + memory map + device instances by
  component-id + params + IRQ wiring + clock + fastmem/MMIO + `TimingTier`); device BEHAVIOR stays code (`IPeripheral`
  components in a registry, `MACHGEN0NN` diagnostics mirroring CPUGEN); behavioral bank-switching = code "mapper"
  components the manifest references; no Turing-complete DSL. ROM refs = path + sha256 (hash-verified). Loader = pure
  front-end to `MachineBuilder` → identical `Machine` (`Breadboard6502` = parity reference). **Decision 8 (owner-required
  at acceptance) — ARTIFACT-INGESTION TOOLING:** an acquire→convert→verify→cache pipeline turning user-supplied ROMs +
  disk images (ATR/D64/IMG/DSK, headered/interleaved/byte-swapped ROMs) into raw bytes (ROM) + raw sectors
  (`IBlockDevice`); `ArtifactAdapterRegistry` (id→converter); cache `~/.cache/cpuemulator/artifacts` (content-addressed,
  `CPUEMULATOR_ARTIFACTS` override); cross-platform .NET tool (NOT PowerShell); copyright-clean (converts user artifacts,
  never bundles ROMs); manifest `--check`/`--prepare`. **Loader + tooling land at SP1** (YAGNI for SP0's one demo
  machine); SP0 contracts designed config-READY. 13 open questions for the SP1 planner.
5. After M4.5d-2 + M4.6: **benchmarking Milestone B** (68000 baseline — gate its CYCLE numbers on M4.5d-2 per ADR §6)
   → **M5** (8086) → **M6** (hot-op JIT emit = the benchmarking before/after speedup payoff).

## Standing rules (carry forward)
- **Merge permission:** the owner authorized merging PRs that pass test + automated UAT (the TomHarte sweep) + review
  — BUT foundational/seam-breaking PRs (like #44) are still held for the owner's personal review.
- **Always branch + PR** (never commit implementation to main). Plans/ADRs/docs may go straight to main.
- Builders STOP before merge; the Coordinator independently verifies, then merges (or holds, for seam-breaking work).
- Heavy gates SEQUENTIAL under `-c Release`; kill stray `testhost.exe`/`dotnet` between runs.

## 🔧 Infra — the empty-spawn glitch (and the mitigation already applied)
- Symptom: subagents intermittently return 0 tool uses + echo skill/agent-teams boilerplate (a no-op), worsening as a
  session's context grows ("late in session"). Re-dispatch fresh recovers (repo untouched on a 0-tool-use no-op).
- **Mitigation applied:** removed `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` from `~/.claude/settings.json` (every glitched
  spawn echoed agent-teams boilerplate — the likely amplifier). Takes effect THIS fresh session.
- If it still recurs: consult the `claude-code-guide` agent on the mechanism, or fall back to Coordinator-drives-in-loop
  (spawn-free) for planning, as was done for the M4.5d-2 plan.
- **2026-06-16 update:** the Designer→Planner→Builder spawns for PR #45 hit a DIFFERENT failure mode — a transient
  API 500 then 529 Overloaded at the spawn layer (an Anthropic-side incident, not the empty-spawn glitch), while
  main-loop calls kept working. The **Coordinator-drives-in-loop (spawn-free)** fallback was used for the ENTIRE
  implementation (not just planning) and worked cleanly end-to-end. Takeaway: when spawns fail but the main loop is
  healthy, drive the work inline — it's a viable path for implementation, not only planning.
- Housekeeping: a stale dir `.claude/worktrees/agent-aaa50d8a03e944c20` may physically linger (deregistered from git,
  a dotnet process held a file lock) — `rm -rf` it once the old session's processes are dead.
